define([
    'app'
],
    function (app) {
        'use strict';
        app.controller('UserRequestsController', [
            '$rootScope','$scope',
            function ($rootScope, $scope) {

            }]); // end of controller
    }); // end of define
