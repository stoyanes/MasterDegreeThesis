define([
        'app',
    ],
    function (app) {
        'use strict';
        app.controller('HomeController', [
            '$scope',
            function ($scope) {
                
                $scope.eventsData = [];

            }]); // end of controller

    }); // end of define